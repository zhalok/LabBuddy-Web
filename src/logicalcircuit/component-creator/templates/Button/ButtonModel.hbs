import { BaseModel } from '../../core';

export default class {{fileName name}}Model extends BaseModel {
  initialize() {
    this.addOutputPort('out');
  }

  onSimulationStart() {
    this.emit({ out: 0 });
  }

  onClick() {
    this.emit({ out: 1 });
  }

  onRelease() {
    this.emit({ out: 0 });
  }
}
